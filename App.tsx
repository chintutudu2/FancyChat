import {SafeAreaView} from 'react-native';
import React from 'react';
import {Chat} from '@screens/index';

const App = () => {
  return (
    <SafeAreaView>
      <Chat />
    </SafeAreaView>
  );
};

export default App;
